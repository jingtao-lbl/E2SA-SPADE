# SPADE core dependencies
pydantic>=2.5
duckdb>=0.10
lancedb>=0.10
pyarrow>=15
pyyaml>=6
anthropic>=0.30
click>=8.1
requests>=2.31

# Geospatial stack (harmonization, regridding, raster + vector I/O)
xarray>=2024.1
rioxarray>=0.15
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
matplotlib>=3.8

# Optional: multi-platform literature search backend
# (Semantic Scholar HTTP backend works without this; flip to MCP via
# `python -m e2sa.agents.litreview ... --backend mcp` to enable.)
paper-search-mcp

# Dev / testing
pytest>=8
